SURVIVED refuter 2026-09-02 identical add -f/unchanged edit/identical copy -f/same-tree 2-machine merge each publish rc0 and the next honest commit+record publish after each; empty-tree root refused as 2nd parent AND as the record's own root (first publish rc1); remove-all empty non-root publishes and next commit follows; empty non-root 2nd parent off remote tip publishes (no gain); legacy empty root already on remote does not lock out; negative control on f580baf^ reproduces the lockout so the tests are not vacuous; 71/71 baits; fresh-HOME all.sh rc0 0 FAIL 4 SKIP on a clone at 3adaba7; leak grep clean, author noreply; Den ~/.git-hooks at f580baf clean; residue: an empty own root locks that record out permanently (no honest git notes op produces one, remedy is a rewrite); commit messages of empty non-root record commits are an unchecked text channel (pre-existing, same-privilege); sha256 repos and octopus merges not exercised; Den verified by sha only, no live push there
