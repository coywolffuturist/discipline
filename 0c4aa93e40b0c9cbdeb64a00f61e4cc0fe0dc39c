REFUTED refuter 2026-09-02 n==0 in is_notes_record locks the record out: git notes add -f (same text twice), edit (unchanged), copy -f (identical), merge (identical trees) each write an empty-diff commit; premise 'git notes never writes an empty commit' is false
